# OpenAI live query mode
# Required only if using Provider = "OpenAI live"
OPENAI_API_KEY=

# Optional PageSpeed Insights API
# Required only if collecting PageSpeed metrics for live URLs
PAGESPEED_API_KEY=

# Optional local SQLite path override
SEO_SUITE_DB_PATH=data/seo_suite.db

